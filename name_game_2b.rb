def palindrome?(the_name)
	the_name.downcase == the_name.downcase.reverse
end

def instructor?(the_name)
	the_name == "Dan"
end

def ta?(the_name)
	the_name == 
	
end

def prompt(text)
	print text
	gets.chomp
end

name = prompt('What is your name? ')

for 

def palindrome?(text)
	if name.downcase == name.downcase.reverse
		puts "#{name} is a palindrome."
	else puts "#{name} is not."
	end
end

for text in output do
	output +=palindrome
	
end




